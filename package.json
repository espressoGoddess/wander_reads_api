{
  "name": "wander_reads_api",
  "version": "1.0.0",
  "scripts": {
    "db:migrate": "sequelize db:migrate",
    "db:seed": "sequelize db:seed:all",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run db:migrate && nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.32.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "sequelize-cli": "^6.6.1"
  }
}
